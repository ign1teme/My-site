# 枝海视觉系统

## 1. 氛围

安静的北境文学刊物：纸感、墨色、融雪与一处樱粉。文字是主体，插画承担情绪与辨识，不把页面做成通用卡片作品集。

## 2. 色彩

浅色画布 `oklch(0.972 0.011 78)`，深色画布 `oklch(0.17 0.012 255)`；正文墨色分别为 `oklch(0.225 0.018 255)` 与 `oklch(0.91 0.012 78)`。樱粉强调色为 `oklch(0.50 0.13 12)`，深色模式为 `oklch(0.72 0.12 12)`。小字号文字与画布、普通和凸起表面均保持至少 4.5:1 对比度。同一页面不进行局部反色。

## 3. 字体

中文正文与标题使用仓库打包的 Noto Serif SC，界面操作使用系统无衬线字体。标题采用轻字重与紧字距，正文采用较宽行距和 720px 最大阅读宽度。

## 4. 组件

交互控件只使用 2px 小圆角。主按钮为墨色底、纸色字；链接拥有明确的下划线或位移反馈。焦点轮廓为樱粉，点击反馈只做轻微缩放。小说封面不使用浮动卡片边框或阴影。

## 5. 排版

首页首屏左右分栏，标题与图片形成一次强对位；博客用时间轴式行布局；小说以大小不等的两幅封面形成错位；关于段落采用宽窄双栏。每一段落布局只出现一次。

## 6. 层次

依靠纸面色阶、细分隔线与留白，而非大面积阴影。深色模式以低亮度层次区分画布和次级表面。

## 7. 守则

保留“枝海”及现有内容 URL；不引入外部字体；不加入装饰性落樱或滚动提示；不制造假数字和假证言；不在首屏堆叠额外信息；所有图片要有明确尺寸和替代文本。

## 8. 响应式

小于 768px 时，首屏、小说、关于和小说详情明确降为单栏。导航保持一行，交互目标至少 40px。375px 是最低验收宽度，页面不得横向溢出。所有动画遵守减少动态效果偏好。

## 9. 后续页面提示

- 新博客详情：画布 `var(--canvas)`，正文宽度 `var(--reading-width)`，标题用 `var(--font-display)`、400 字重、`-0.035em` 字距，正文维持约 2 倍行高。
- 新作品列表：沿用大小不等的真实图片与独立描述，不复制等宽三卡布局；状态文字用 `var(--accent)`，所有图片提供宽高与替代文本。
- 新交互：采用 `var(--radius-control)`、44px 最小触控高度、`:focus-visible` 樱粉描边；只动画 `transform` 和 `opacity`。
